{
  "name": "shoptelgram",
  "version": "1.0.0",
  "description": "A complete Telegram shop bot running on Cloudflare Workers + KV.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create SHOP"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
